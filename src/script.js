// selecting elements
var aElements = document.querySelectorAll("a");
// gelobal varibales
var elementsCount = 0;
// functions
// event linsters
aElements.forEach(function (aElement) {
    console.log(aElement);
});
