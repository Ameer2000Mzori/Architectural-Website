// selecting elements
const aElements = document.querySelectorAll("a");

// gelobal varibales
let elementsCount = 0;

// functions

// event linsters
aElements.forEach((aElement) => {
  console.log(aElement);
});
